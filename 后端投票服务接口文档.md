# 后端投票服务 API 接口文档

## 概述
本文档面向 `vote` 模块，整理了当前后端已实现的投票接口：投票活动创建、投票提交、结果查询，以及管理端恢复/快照/DLQ 重试接口。  
接口统一前缀为 `/api/votes`，返回 JSON。

---

## 1. REST API 接口

### 1.1 创建投票活动 (Create Poll)
创建一个新的投票活动，并初始化 Redis 计数器。

**URL**: `/api/votes/polls`  
**Method**: `POST`

**Request Body**:
```json
{
  "name": "最佳方案评选",
  "allowMultiple": false,
  "options": ["方案A", "方案B", "方案C"]
}
```

**字段说明**:
- `name` (String, 必填, max=128): 投票标题
- `allowMultiple` (Boolean, 必填): 是否允许多选
- `options` (String[], 必填, 2~30 项): 候选项列表；每项不能为空、max=128

**返回 (`VotePollCreateResponse`)**:
```json
{
  "pollId": 1,
  "name": "最佳方案评选",
  "allowMultiple": false,
  "options": [
    { "optionId": 11, "optionText": "方案A", "votes": 0 },
    { "optionId": 12, "optionText": "方案B", "votes": 0 },
    { "optionId": 13, "optionText": "方案C", "votes": 0 }
  ]
}
```

---

### 1.2 提交投票 (Submit Vote)
提交一次投票请求，服务端先写 Redis 并受理，再通过 Outbox + Stream 异步持久化。

**URL**: `/api/votes/submit`  
**Method**: `POST`

**Request Body**:
```json
{
  "pollId": 1,
  "voterId": "user-1001",
  "optionIds": [11]
}
```

**字段说明**:
- `pollId` (Long, 必填): 投票活动 ID
- `voterId` (String, 必填): 投票人标识
- `optionIds` (Long[], 必填): 选择的选项 ID（单选活动只能传 1 个）

**返回 (`VoteSubmitResponse`)**:
```json
{
  "eventId": "e16964d4-4c43-4e8d-9e46-4552ab841ec1",
  "pollId": 1,
  "acceptedAt": "2026-05-18T03:12:01.123Z"
}
```

**接口特性**:
1. 用户 + IP 双维度滑动窗口限流（默认 1 秒内最多 1 次）。
2. 使用 Redlock 风格分布式锁保护并发提交。
3. Redis Lua 原子校验选项合法性并更新票数。
4. 若事务消息写入失败，会回滚本次 Redis 计数。

---

### 1.3 查询投票结果 (Get Poll Result)
查询某投票活动的汇总票数与选项票数。优先读 Redis，必要时回退数据库聚合结果。

**URL**: `/api/votes/polls/{pollId}/result`  
**Method**: `GET`

**Path Parameters**:
- `pollId` (Long): 投票活动 ID

**返回 (`VotePollResultResponse`)**:
```json
{
  "pollId": 1,
  "name": "最佳方案评选",
  "allowMultiple": false,
  "ballots": 128,
  "options": [
    { "optionId": 11, "optionText": "方案A", "votes": 52 },
    { "optionId": 12, "optionText": "方案B", "votes": 43 },
    { "optionId": 13, "optionText": "方案C", "votes": 33 }
  ]
}
```

---

### 1.4 重建 Redis 计数 (Admin Rebuild)
基于快照 + 事件回放重建 Redis 计数。

**URL**: `/api/votes/admin/recovery/rebuild`  
**Method**: `POST`

**返回**: `int`（本次重建的投票活动数量）

---

### 1.5 创建快照 (Admin Snapshot)
立即触发一次快照任务，将聚合计数写入 `vote_count_snapshot`。

**URL**: `/api/votes/admin/snapshot`  
**Method**: `POST`

**返回**: `200 OK`（无返回体）

---

### 1.6 重试死信消息 (Admin DLQ Retry)
将 DLQ Stream 内消息回投到主 Stream，并删除已重试消息。

**URL**: `/api/votes/admin/dlq/retry`  
**Method**: `POST`

**Query Parameters**:
- `limit` (Int, 可选, 默认 100): 本次最多重试条数，最小值会被修正为 1

**返回 (`DlqRetryResponse`)**:
```json
{
  "retriedCount": 37
}
```

---

## 2. 错误响应格式

**统一格式 (`VoteErrorResponse`)**:
```json
{
  "code": "RATE_LIMITED",
  "message": "请求过快，请 1 秒后重试",
  "timestamp": "2026-05-18T03:15:12.456Z"
}
```

**常见错误码**:

| code | HTTP 状态码 | 说明 |
|---|---|---|
| VALIDATION_ERROR | 400 | 参数校验失败 |
| INVALID_OPTIONS | 400 | 创建活动时选项数量不足或重复 |
| POLL_NOT_FOUND | 404 | 投票活动不存在 |
| POLL_CLOSED | 409 | 投票活动已关闭 |
| INVALID_OPTION_COUNT | 400 | 单选活动提交了多个选项 |
| OPTION_REQUIRED | 400 | 未选择任何选项 |
| INVALID_OPTION | 400 | 选项 ID 非法 |
| RATE_LIMITED | 429 | 触发限流 |
| REDLOCK_ACQUIRE_FAILED | 409 | 并发锁竞争失败 |
| EVENT_WRITE_FAILED | 500 | 事务消息保存失败，投票已回滚 |
| MQ_DISABLED | 409 | MQ 未启用，无法执行 DLQ 重试 |

---

## 3. 联调示例 (curl)

**1) 创建投票活动**
```bash
curl -X POST "http://localhost:8080/api/votes/polls" ^
  -H "Content-Type: application/json" ^
  -d "{\"name\":\"最佳方案评选\",\"allowMultiple\":false,\"options\":[\"方案A\",\"方案B\",\"方案C\"]}"
```

**2) 提交投票**
```bash
curl -X POST "http://localhost:8080/api/votes/submit" ^
  -H "Content-Type: application/json" ^
  -d "{\"pollId\":1,\"voterId\":\"user-1001\",\"optionIds\":[11]}"
```

**3) 查询结果**
```bash
curl -X GET "http://localhost:8080/api/votes/polls/1/result"
```

**4) 管理接口示例**
```bash
curl -X POST "http://localhost:8080/api/votes/admin/recovery/rebuild"
curl -X POST "http://localhost:8080/api/votes/admin/snapshot"
curl -X POST "http://localhost:8080/api/votes/admin/dlq/retry?limit=100"
```

---

## 4. WebSocket 说明
当前 `vote` 模块未提供专用的投票结果 WebSocket 推送接口，前端可通过轮询  
`GET /api/votes/polls/{pollId}/result` 获取实时统计结果。
