# 分布式大屏抽奖与一致性投票系统 - 概要设计文档

## 1. 系统架构

本系统采用前后端分离的微服务架构（单体模块化），前端 Vue 3 + Vite，后端 Spring Boot 3。核心数据流向如下：
[浏览器/Vue前端]
│ (HTTP/WebSocket)
▼
[Spring Boot 后端]
├── Redis (缓存、分布式锁、计数器、抽奖池、Pub/Sub)
├── MySQL (持久化存储、快照、历史记录)
└── Redis Stream (异步事件、Outbox、死信队列)

**关键设计原则：**
- **强一致性路径**：抽奖、接龙参与、投票提交 → 使用分布式锁 / Lua / SPOP + 事务回滚
- **最终一致性路径**：问卷提交 → 直写 Redis + Outbox 异步落库
- **读写分离**：投票统计读 Redis，必要时回退 MySQL

## 2. 模块划分

| 模块 | 包路径 | 核心类 |
|------|--------|--------|
| 问卷 | `com.example.distributed.quest` | `QuestionnaireService`, `QuestionnaireController` |
| 接龙 | `com.example.distributed.chain` | `ChainService`, `ChainController`, `DistributedLockService` |
| 抽奖 | `com.example.distributed.lottery` | `LotteryService`, `LotteryController`, `WebSocketConfig` |
| 投票 | `com.example.distributed.vote` | `VoteService`, `VoteRecoveryService`, `VoteEventConsumer` |

## 3. 数据库设计概要

### 3.1 核心实体关系

- **问卷**：`questionnaire` (1) ←→ (N) `question` (1) ←→ (N) `question_option`
- **接龙**：`chain` (1) ←→ (N) `chain_entry`
- **抽奖**：`lottery_activity` (1) ←→ (N) `lottery_history`
- **投票**：`vote_poll` (1) ←→ (N) `vote_option` ←→ (N) `vote_record`

### 3.2 关键索引与约束

- 问卷答卷：`UNIQUE(questionnaire_id, user_id)`
- 接龙项：`UNIQUE(chain_id, user_id)`，`INDEX(chain_id, sequence_no)`
- 中奖历史：`UNIQUE(activity_id, user_id)`
- 投票快照：`INDEX(poll_id, snapshot_date)`

## 4. 接口概要

### 4.1 问卷接口 (前缀 `/api/questionnaires`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/` | 创建问卷 |
| GET | `/active` | 获取活跃问卷列表 |
| GET | `/{id}` | 获取问卷详情 |
| POST | `/submit` | 提交答卷 |
| GET | `/{id}/statistics` | 获取统计 |

### 4.2 接龙接口 (前缀 `/api/chains`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/` | 创建接龙 |
| GET | `/active` | 获取活跃接龙 |
| GET | `/{id}` | 获取接龙详情及所有项 |
| POST | `/{id}/join` | 参与接龙 |
| DELETE | `/{id}` | 删除接龙 |

### 4.3 抽奖接口 (前缀 `/api/lottery`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/activities` | 获取活动列表 |
| POST | `/{activityId}/join` | 加入奖池 |
| POST | `/{activityId}/draw` | 执行抽奖 |
| GET | `/{activityId}/winners/latest` | 最新中奖名单 |
| GET | `/{activityId}/history` | 中奖历史分页 |

### 4.4 投票接口 (前缀 `/api/votes`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/polls` | 创建投票 |
| POST | `/submit` | 提交投票 |
| GET | `/polls/{pollId}/result` | 查询结果 |
| GET | `/polls?activeOnly=true` | 投票活动列表 |
| GET | `/polls/{pollId}/submitted` | 查询用户是否已投票 |
| POST | `/admin/recovery/rebuild` | 重建 Redis 计数 |
| POST | `/admin/snapshot` | 创建快照 |
| POST | `/admin/dlq/retry` | 重试死信 |

### 4.5 WebSocket 端点

- 连接地址：`ws://{host}:8080/ws` (STOMP)
- 订阅频道：`/topic/lottery/winners/{activityId}`

## 5. 关键技术选型

| 技术 | 用途 |
|------|------|
| Redis Set + SPOP | 原子抽奖、无超发 |
| Redis SETNX | 分布式锁（接龙、投票） |
| Redis Stream | 异步事件、Outbox、死信队列 |
| Redis Pub/Sub | WebSocket 跨实例推送 |
| Lua 脚本 | 原子校验选项合法性并更新票数 |
| Redisson | 强一致分布式锁（投票模块） |
| JPA @Version | 乐观锁（问卷、接龙） |
| 唯一索引 | 数据库层防重兜底 |

## 6. 分布式策略总结

| 场景 | 方案 |
|------|------|
| 防止重复提交 | Redis 幂等 key + DB 唯一约束 |
| 抽奖防超发 | Redis SPOP + 两阶段落库 + 回滚补偿 |
| 接龙序号连续性 | 分布式锁串行化 + 数据库序列号 |
| 投票并发控制 | Redlock + Lua 原子操作 |
| 大屏查询一致性 | 读写锁（ReadWriteLock） |
| Redis 宕机恢复 | 快照 + 事件回放重建 |