# 分布式大屏抽奖与一致性投票系统 - 测试报告

## 1. 测试环境

- **后端服务**：Spring Boot 3 运行于 Docker 容器（4核8G），MySQL 8.0，Redis 6.2
- **前端**：Vue 3 + Vite 开发服务器
- **测试工具**：JMeter 5.5（并发），Postman（接口），自定义 PowerShell 脚本（抽奖）
- **网络**：本地局域网延迟 < 2ms

## 2. 测试范围

| 模块 | 测试类型 | 覆盖点 |
|------|----------|--------|
| 问卷 | 功能、并发、边界 | 创建、提交、防重复、活跃列表、统计 |
| 接龙 | 功能、并发、顺序 | 创建、参与、序号连续、防重复、删除 |
| 抽奖 | 功能、并发、容错 | 加入池、抽奖原子性、超卖边界、WebSocket 推送 |
| 投票 | 功能、灾备、一致性 | 创建、提交、结果查询、快照重建、死信重试 |

## 3. 测试用例与结果

### 3.1 问卷模块

| 用例 | 步骤 | 预期 | 结果 |
|------|------|------|------|
| 正常提交 | 创建问卷，用户提交答案 | 返回成功，统计增加 | ✅ |
| 重复提交 | 同一用户再次提交同一问卷 | 拒绝（409 或唯一约束） | ✅ |
| 活跃列表时区问题 | 开始时间为当前时间前10秒，查询活跃列表 | 列表包含该问卷 | ✅（已修复时区） |
| 最大提交次数 | 设置 maxSubmissions=1，用户提交两次 | 第二次失败 | ✅ |

### 3.2 接龙模块

| 用例 | 步骤 | 预期 | 结果 |
|------|------|------|------|
| 并发参与序号 | 100 个用户同时参与同一接龙 | 序号 1~100 连续不重复 | ✅ |
| 重复参与 | 同一用户再次参与（allowMultiple=false） | 拒绝（409） | ✅ |
| 人数上限 | 设置 maxParticipants=50，第51人参与 | 拒绝（400） | ✅ |
| 删除接龙 | 创建者删除接龙 | 接龙不再出现于列表 | ✅ |

### 3.3 抽奖模块

| 用例 | 步骤 | 预期 | 结果 |
|------|------|------|------|
| 正常抽奖 | 奖池 30 人，抽取 5 人 | 返回 5 个不同用户 | ✅ |
| 超卖边界 | 奖池 30 人，抽取 50 人 | 返回 30 人（无空气人） | ✅ |
| 一人中多奖 | 同一用户重复入池，抽中一次后再次被抽中 | 数据库唯一约束拦截，用户被回滚至奖池 | ✅ |
| WebSocket 推送 | 抽奖后监听 /topic/lottery/winners/{id} | 收到 JSON 数组 | ✅（延迟 <50ms） |

### 3.4 投票模块

| 用例 | 步骤 | 预期 | 结果 |
|------|------|------|------|
| 并发提交同一选项 | 100 个用户同时为同一选项投票 | 票数 = 100，无超投 | ✅ |
| 单选活动多选 | 提交多个 optionIds | 返回 400 | ✅ |
| Redis 全毁重建 | 执行 FLUSHALL，调用 rebuild 接口 | 票数与重建前完全一致 | ✅ |
| 死信重试 | 模拟消费异常，消息进入 DLQ，调用 retry | 消息重新被消费落库 | ✅ |

## 4. 性能测试摘要

| 场景 | 并发线程 | 平均响应时间 | 错误率 |
|------|----------|--------------|--------|
| 问卷提交 | 200 | 145 ms | 0% |
| 接龙参与 | 200 | 162 ms | 0% |
| 抽奖（draw） | 50 | 89 ms | 0% |
| 投票提交 | 300 | 210 ms | 0% |
| WebSocket 消息推送 | 100 个订阅者 | 32 ms 广播延迟 | 0% |

## 5. 缺陷与修复记录

| ID | 描述 | 严重程度 | 状态 |
|----|------|----------|------|
| BUG-01 | 活跃问卷/接龙列表为空（时区错配） | 高 | 已修复（统一 Asia/Shanghai） |
| BUG-02 | 抽奖并发下偶有唯一键冲突未回滚 | 中 | 已修复（添加事务回滚补偿） |
| BUG-03 | 接龙序号在极低概率下重复 | 中 | 已修复（分布式锁超时延长） |

## 6. 测试结论

- ✅ 所有核心功能符合需求规约
- ✅ 并发场景下无超发、无重复、序号连续
- ✅ Redis 灾备恢复机制有效，数据零丢失
- ✅ WebSocket 推送稳定，多屏同步实时
- ⚠️ 建议后续增加网关层限流及消息队列升级

**测试完成日期：** 2026年6月2日  
**测试负责人：** 项目组