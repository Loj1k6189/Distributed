# 分布式大屏抽奖与一致性投票系统 - 需求规约

## 1. 项目背景

本项目为“分布式大屏抽奖与一致性投票系统”，面向高并发、多终端实时同步场景，旨在提供一个支持**问卷填报**、**接龙互动**、**大屏抽奖**及**强一致性投票**的综合性互动平台。系统需保证数据不丢失、不超发、实时推送，并具备良好的水平扩展能力。

## 2. 业务目标

- 支持千人级并发提交，核心操作响应时间 < 200ms
- 抽奖、投票等核心业务做到无超发、无重复
- WebSocket 推送延迟 < 50ms，多屏同步
- 支持 Redis 宕机后从 MySQL 无损重建

## 3. 用户角色

| 角色 | 描述 |
|------|------|
| 普通用户 | 参与问卷、接龙、投票；查看抽奖大屏 |
| 活动创建者 | 创建问卷、接龙、投票活动；管理抽奖活动 |
| 管理员 | 查看统计、重建缓存、重试死信消息 |

## 4. 功能性需求

### 4.1 问卷模块 (Questionnaire)

| 编号 | 需求描述 |
|------|----------|
| Q-01 | 创建者可以创建问卷，包含标题、描述、题目列表（支持单选/多选/文本/评分/日期）、是否必答、是否允许匿名、最大提交次数、开始/结束时间 |
| Q-02 | 普通用户可以查看活跃问卷列表，选择问卷并填写提交 |
| Q-03 | 系统防止同一用户在允许提交次数内重复提交（数据库唯一约束 + Redis计数器） |
| Q-04 | 创建者可以查看问卷统计（总提交数、各选项票数、平均完成时间） |
| Q-05 | 活跃问卷列表仅显示当前时间在开始与结束之间的问卷（startTime 为空视为立即开始，endTime 为空视为永不结束） |
| Q-06 | 提交时校验问卷是否开始/结束、是否达到最大提交次数 |

### 4.2 接龙模块 (Chain)

| 编号 | 需求描述 |
|------|----------|
| C-01 | 创建者可以创建接龙，包含标题、描述、最大参与人数、是否允许重复参与、开始/结束时间 |
| C-02 | 用户可以查看活跃接龙列表，选择接龙并提交内容（可选回复某条历史接龙） |
| C-03 | 系统保证接龙序号连续不重复，同一用户（若不允许重复参与）无法再次参与 |
| C-04 | 创建者可以查看接龙记录，并通过创建者 ID 删除接龙 |
| C-05 | 接龙参与使用分布式锁保证并发追加时的顺序一致 |

### 4.3 大屏抽奖模块 (Lottery)

| 编号 | 需求描述 |
|------|----------|
| L-01 | 管理员可以将用户 ID 加入抽奖池（Set 结构自动去重） |
| L-02 | 管理员可以指定轮次和抽取人数，执行原子抽奖 |
| L-03 | 抽奖结果通过 WebSocket 实时推送给所有大屏客户端 |
| L-04 | 同一用户在整个活动中只能中奖一次（数据库唯一约束兜底） |
| L-05 | 支持分页查询中奖历史记录 |
| L-06 | 若数据库写入失败（如重复中奖），自动将用户回滚至抽奖池 |

### 4.4 投票模块 (Vote)

| 编号 | 需求描述 |
|------|----------|
| V-01 | 创建者可以创建投票活动（标题、是否允许多选、候选项列表） |
| V-02 | 用户可以提交投票（单选/多选），系统使用分布式锁防止超投 |
| V-03 | 投票结果优先从 Redis 读取，支持查询实时票数 |
| V-04 | 系统支持管理员重建 Redis 计数（基于快照 + 增量事件） |
| V-05 | 系统定时创建票数快照，支持死信队列重试 |
| V-06 | 使用读写锁保证大屏查询结果的一致性，避免脏读 |

## 5. 非功能性需求

| 类别 | 要求 |
|------|------|
| 性能 | 单接口支持 500 QPS，WebSocket 推送延迟 < 50ms |
| 一致性 | 抽奖、投票、接龙序号强一致；问卷统计最终一致 |
| 可用性 | Redis 宕机后可通过管理接口从 MySQL 恢复，RTO < 5 分钟 |
| 安全性 | 用户 ID 通过 Header 传递，支持匿名提交，防重放攻击 |
| 可扩展性 | 支持 Docker 容器化部署，可横向扩展后端实例 |